import React from "react";
import PropTypes from "prop-types";
import key from "weak-key";

class Home extends Component {
    render() {
        return (
            <div>
                <h2>Example home</h2>
                <p>This page will be used for displaying
                     subscription updates, and suggestions</p>
            </div>
        );
    }
}

export default Home;